package ohnosequences.statika.aws.tests

import ohnosequences.statika._
import ohnosequences.statika.aws._
import org.scalatest._

class AMITests extends FunSuite {

  // test("Special metadata in AMI") {
    // println(dist.ami.userScript(dist, bun))
    // println(dist.userScript[dist.type, bun.type](bun))
    // println(dist.userScript(bun))
  // }

}
